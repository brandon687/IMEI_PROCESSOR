╔═══════════════════════════════════════════════════════════════════════════════╗
║                    HAMMER-API ARCHITECTURE OVERVIEW                           ║
║                   Order History & Database Integration                        ║
╚═══════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│                            USER INTERFACE (Browser)                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │  Submit  │  │  Batch   │  │ History  │  │ Database │  │  Search  │    │
│  │  Order   │  │  Upload  │  │  View    │  │   View   │  │  Orders  │    │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘    │
│       │             │              │             │              │           │
└───────┼─────────────┼──────────────┼─────────────┼──────────────┼───────────┘
        │             │              │             │              │
        ▼             ▼              ▼             ▼              ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                         FLASK WEB APP (web_app.py)                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  /submit          /batch            /history         /database              │
│  ├─ POST          ├─ POST           ├─ GET           ├─ GET                 │
│  │  1. Parse      │  1. Read CSV    │  1. Query DB   │  1. Get stats       │
│  │  2. Call API   │  2. Parse rows  │  2. Format     │  2. Recent orders   │
│  │  3. Store DB   │  3. Call API    │  3. Display    │  3. Display         │
│  │                │  4. Store DB    │                │                      │
│  │                │                 │                │                      │
│  /history/sync    /status/<id>     /database/search /database/export        │
│  ├─ GET           ├─ GET            ├─ GET           ├─ GET                 │
│  │  1. Get pending│  1. Call API    │  1. Query DB   │  1. Query DB        │
│  │  2. Batch API  │  2. Show real-  │  2. Search     │  2. Generate CSV    │
│  │  3. Update DB  │     time data   │  3. Display    │  3. Download        │
│                                                                              │
├──────────────────────┬──────────────────────────┬────────────────────────────┤
│                      │                          │                            │
│                      ▼                          ▼                            │
│        ┌──────────────────────────┐  ┌──────────────────────────┐          │
│        │   GSM FUSION CLIENT      │  │   DATABASE MODULE        │          │
│        │  (gsm_fusion_client.py)  │  │    (database.py)         │          │
│        └──────────────────────────┘  └──────────────────────────┘          │
│                      │                          │                            │
└──────────────────────┼──────────────────────────┼────────────────────────────┘
                       │                          │
                       ▼                          ▼
         ┌─────────────────────────┐  ┌─────────────────────────┐
         │   GSM FUSION API        │  │   SQLite DATABASE       │
         │  (hammerfusion.com)     │  │  (imei_orders.db)       │
         ├─────────────────────────┤  ├─────────────────────────┤
         │                         │  │                         │
         │ • Submit IMEI orders    │  │ • orders table          │
         │ • Get order status      │  │ • Full order history    │
         │ • Batch status checks   │  │ • Search/filter         │
         │ • Service list          │  │ • Export capability     │
         │                         │  │                         │
         │ Remote Storage          │  │ Local Storage           │
         │ (Authoritative)         │  │ (Mirror/Cache)          │
         └─────────────────────────┘  └─────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
                              DATA FLOW EXAMPLES
═══════════════════════════════════════════════════════════════════════════════

EXAMPLE 1: Submit Single Order
───────────────────────────────

User enters IMEI → Web Form → Flask /submit
                                  │
                   ┌──────────────┴──────────────┐
                   ▼                             ▼
            GSM Fusion API                 Local Database
       (place_imei_order)                (insert_order)
                   │                             │
                   ├─ Returns order_id          │
                   │  Status: "Pending"          │
                   │                             │
                   └─────────────┬───────────────┘
                                 ▼
                          Redirect to /status
                          Show order details


EXAMPLE 2: View Order History
──────────────────────────────

User clicks "Order History" → Flask /history
                                    │
                                    ▼
                              Database Query
                         (get_recent_orders(100))
                                    │
                                    ▼
                            Format & Display
                     ┌─────────────────────────┐
                     │ Order ID │ IMEI │ Status │
                     ├─────────────────────────┤
                     │ 12345678 │ 3521...│ Done  │
                     │ 12345679 │ 3521...│ Pend. │
                     │ 12345680 │ 3521...│ Done  │
                     └─────────────────────────┘
                              ▲
                              │
                         (No API call!
                          Fast, always
                          available)


EXAMPLE 3: Sync Order Status
─────────────────────────────

User clicks "🔄 Sync Status" → Flask /history/sync
                                      │
                                      ▼
                         1. Query Database for Pending
                         (search_orders_by_status)
                                      │
                    ┌─────────────────┴─────────────────┐
                    │ order_ids = [12345679, 12345681]  │
                    └─────────────────┬─────────────────┘
                                      ▼
                         2. Batch API Call
                    client.get_imei_orders([12345679, 12345681])
                                      │
                    ┌─────────────────┴─────────────────┐
                    │ Returns:                          │
                    │   12345679 → "Completed"          │
                    │   12345681 → "Completed"          │
                    └─────────────────┬─────────────────┘
                                      ▼
                         3. Update Database
                  UPDATE orders SET status='Completed'
                  WHERE order_id IN (12345679, 12345681)
                                      │
                                      ▼
                          4. Redirect to /history
                          (Now shows updated status)


═══════════════════════════════════════════════════════════════════════════════
                           DATABASE SCHEMA
═══════════════════════════════════════════════════════════════════════════════

┌────────────────────────────────────────────────────────────────────────┐
│                           TABLE: orders                                │
├───────────────────┬─────────────────┬──────────────────────────────────┤
│ Column            │ Type            │ Description                      │
├───────────────────┼─────────────────┼──────────────────────────────────┤
│ id                │ INTEGER PK      │ Auto-increment primary key       │
│ order_id          │ TEXT UNIQUE     │ GSM Fusion order ID              │
│ service_name      │ TEXT            │ Service display name             │
│ service_id        │ TEXT            │ Service package ID               │
│ imei              │ TEXT NOT NULL   │ Device IMEI (15 digits)          │
│ imei2             │ TEXT            │ Dual-SIM IMEI2                   │
│ credits           │ REAL            │ Cost in credits                  │
│ status            │ TEXT            │ Pending/Completed/Rejected       │
│ carrier           │ TEXT            │ Result: Carrier name             │
│ simlock           │ TEXT            │ Result: Lock status              │
│ model             │ TEXT            │ Result: Device model             │
│ fmi               │ TEXT            │ Result: Find My iPhone           │
│ order_date        │ TIMESTAMP       │ When order was placed            │
│ result_code       │ TEXT            │ Result code (SUCCESS, etc.)      │
│ notes             │ TEXT            │ Additional notes                 │
│ raw_response      │ TEXT            │ Full API response (JSON)         │
│ created_at        │ TIMESTAMP       │ Record creation time             │
│ updated_at        │ TIMESTAMP       │ Last update time                 │
└───────────────────┴─────────────────┴──────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
                          API INTEGRATION DETAILS
═══════════════════════════════════════════════════════════════════════════════

GSM Fusion API Endpoints Used:
───────────────────────────────

1. GET /imei-services
   └─> Returns: List of available IMEI services
       Used by: /services, /submit (service dropdown)

2. POST /placeimeiorder
   Parameters: imei, network_id
   └─> Returns: order_id, status
       Used by: /submit, /batch

3. GET /getimeis
   Parameters: orderIds (comma-separated)
   └─> Returns: List of order details with status
       Used by: /status/<id>, /history/sync

Batch Support:
──────────────
✅ API accepts multiple order IDs: "12345678,12345679,12345680"
✅ Single API call returns all statuses
✅ Reduces API calls by 100x for syncing

Rate Limits:
────────────
• Typical: 60 requests/minute
• Batch requests count as 1 request
• Status checks usually unlimited/free


═══════════════════════════════════════════════════════════════════════════════
                         PERFORMANCE METRICS
═══════════════════════════════════════════════════════════════════════════════

Operation                    | Speed         | API Calls | Cost
─────────────────────────────┼───────────────┼───────────┼─────────────
Submit 1 order               | ~500ms        | 1         | 1 credit
Submit 100 orders (batch)    | ~30-60s       | 100       | 100 credits
View history (100 orders)    | ~50ms         | 0         | Free
Sync 100 pending orders      | ~1-2s         | 1 (batch) | Free
Search database (IMEI)       | ~10ms         | 0         | Free
Export 1000 orders to CSV    | ~200ms        | 0         | Free

Key Optimization: Database caching reduces API calls by 95%+


═══════════════════════════════════════════════════════════════════════════════
                         FUTURE T-MOBILE INTEGRATION
═══════════════════════════════════════════════════════════════════════════════

When you get T-Mobile API access, you can add parallel data source:

┌─────────────────────────────────────────────────────────────────────────┐
│                      Combined IMEI Verification                          │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  User submits IMEI → Check both sources in parallel                     │
│                                                                          │
│         ┌────────────────┴────────────────┐                            │
│         ▼                                  ▼                            │
│  ┌──────────────┐                  ┌──────────────┐                    │
│  │ GSM Fusion   │                  │ T-Mobile API │                    │
│  │   API        │                  │              │                    │
│  ├──────────────┤                  ├──────────────┤                    │
│  │ • AppleCare  │                  │ • EIP Balance│                    │
│  │ • FMI status │                  │ • Lock Status│                    │
│  │ • GSMA block │                  │ • T-Mo IMEI  │                    │
│  │ • Model info │                  │ • Lease info │                    │
│  └──────────────┘                  └──────────────┘                    │
│         │                                  │                            │
│         └────────────────┬────────────────┘                            │
│                          ▼                                              │
│                  Merge & Store in DB                                    │
│            ┌──────────────────────────────┐                            │
│            │ Combined Result:             │                            │
│            │ • AppleCare: Eligible        │                            │
│            │ • FMI: OFF                   │                            │
│            │ • T-Mobile Lock: Unlocked    │                            │
│            │ • EIP Balance: $0            │                            │
│            │ • GSMA: Clean                │                            │
│            └──────────────────────────────┘                            │
│                                                                          │
│  Same database schema supports both sources!                            │
│  Just add source field: 'GSM_FUSION' or 'TMOBILE_DIRECT'               │
└─────────────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
                              QUICK REFERENCE
═══════════════════════════════════════════════════════════════════════════════

Start Web Server:
    python3 web_app.py

Access URLs:
    http://localhost:5001              - Home
    http://localhost:5001/submit       - Submit order
    http://localhost:5001/batch        - Batch upload
    http://localhost:5001/history      - View order history
    http://localhost:5001/database     - Database management

Database Location:
    /Users/brandonin/Desktop/HAMMER-API/imei_orders.db

Documentation:
    ORDER_HISTORY_GUIDE.md    - Complete technical guide
    WHATS_NEW.md              - What changed and why
    ARCHITECTURE.txt          - This file

Key Files:
    web_app.py               - Flask web application
    gsm_fusion_client.py     - GSM Fusion API client
    database.py              - Database operations
    templates/history.html   - Order history UI

═══════════════════════════════════════════════════════════════════════════════
